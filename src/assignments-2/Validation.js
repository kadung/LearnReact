import React from 'react';

const Validation = (props) => {
    if (this.props.textLength && this.props.textLength>3) {
        alertMessage = 
    }
    

    return (
        <div>
            {this.props.textLength && <p>Please enter the text!</p>}
            
            
        </div>
    )
}

export default Validation;